# Pieria demo — secrets file for deploy/demo/docker-compose.demo.yml's cloudflared service.
#
# Placeholder names only — no real values. Copy this to the path the server's DEMO_SECRETS_FILE
# (or the default /opt/pieria/secrets/demo.env) points at, OUTSIDE this checkout, root-600-style,
# and fill in the real token minted in the Cloudflare Zero Trust dashboard (Access -> Tunnels).
# Never commit the filled-in copy.

TUNNEL_TOKEN=
