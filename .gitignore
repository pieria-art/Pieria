# Environments
venv/
.venv/
ENV/
.env

# Database
*.db
artwork.db
data/

# Media & Assets
Artwork/

# Built art-pack — the bundled distributable (ships via R2 / the .img, per ADR-038); never git.
art-pack/
art-pack-*/

# Python
__pycache__/
*.py[cod]
*$py.class

# System
.DS_Store
.thumbs.db
.coverage

# Test/verification artifacts
.playwright-mcp/
.pytest_cache/

# Catalog builder incremental cache (regenerable)
tools/.catalog_cache.json
tools/.artic_cache.json

# Local-only dev docs (kept on disk, not published).
# .ai/ is private-by-default; only the system architecture doc is tracked.
PRD.md
ROADMAP.md
CLAUDE.md
.ai/*
!.ai/system_architecture.md
