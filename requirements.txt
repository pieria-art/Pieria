# fastapi's OWN pin is what held Starlette at 0.37.2 (CVE-2024-47874, multipart DoS, fixed 0.40.0) —
# bumped together with an explicit Starlette pin so the fix can't regress silently on a re-resolve.
fastapi==0.141.1
starlette==0.52.1
uvicorn==0.30.1
pydantic==2.13.5
# The only range pin in this file; bounded to fastapi's own floor (>=0.0.18) plus an explicit ceiling
# so an appliance that rebuilds itself unattended can't drift onto an untested next-major release.
python-multipart>=0.0.18,<0.1
python-dotenv==1.0.1
sqlalchemy==2.0.30
Pillow==12.3.0
pillow-heif==1.8.0
wikipedia==1.4.0
httpx==0.28.1
alembic==1.13.1
samsungtvws==3.0.5
PyNaCl==1.6.2
# python:3.11-slim ships NO zone data, so zoneinfo.available_timezones() is empty in the container
# and a timezone the user picks can only be shape-checked, never verified (ADR-118/119).
tzdata==2024.1
